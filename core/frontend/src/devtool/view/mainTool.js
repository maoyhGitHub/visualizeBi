// 注 此文件禁止提交，以默认模板为准 devTemplate

// 调试组件时只需修改导入组件路径， @/devtool/view/自己组件文件夹名称/index.vue
import MainIndex from '@/devtool/view/devTemplate/index.vue'
import MainStyle from '@/devtool/view/devTemplate/style.vue'
import MainData from '@/devtool/view/devTemplate/data.vue'

// 以下暴露名称禁止改动，只需修改上面 from 后面路径（改成自己组件路径）
export const mainIndex = MainIndex
export const mainStyle = MainStyle
export const mainData = MainData
